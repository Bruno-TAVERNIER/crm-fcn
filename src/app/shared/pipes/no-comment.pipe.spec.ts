import { NoCommentPipe } from './no-comment.pipe';

describe('NoCommentPipe', () => {
  it('create an instance', () => {
    const pipe = new NoCommentPipe();
    expect(pipe).toBeTruthy();
  });
});
